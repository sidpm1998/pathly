// Frontend Configuration
// 
// This is a template file. Copy this to config.js and add your Supabase keys.
// config.js is in .gitignore and will NOT be committed to the repository.
// 
// IMPORTANT: 
// - OpenAI and ElevenLabs API keys are now stored in the backend (.env file)
// - Only Supabase anon key is needed in the frontend (it's safe to use in browser)

// Supabase Configuration
// Get your anon key from: Supabase Dashboard → Settings → API → anon public key
const SUPABASE_URL = 'YOUR_SUPABASE_URL_HERE';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY_HERE';

